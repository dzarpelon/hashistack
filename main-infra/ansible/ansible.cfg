[defaults]
# Ensure Ansible looks for roles in the repo's ansible/roles directory
roles_path = ./roles

# Use the local inventory by default when running from project root
inventory = ./inventory

# Useful defaults for local lab runs
host_key_checking = False
retry_files_enabled = False
ask_pass = False
forks = 10

[ssh_connection]
pipelining = True
