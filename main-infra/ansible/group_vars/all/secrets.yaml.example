# Secrets Configuration for Kubernetes Infrastructure
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to secrets.yaml in the same directory
# 2. Update all CHANGEME values with your actual credentials
# 3. The secrets.yaml file is gitignored and will not be committed
#
# Example: cp secrets.yaml.example secrets.yaml

# ============================================================================
# Dashboard Authentication
# ============================================================================
# HTTP Basic Auth credentials for protecting dashboards
# Used by: Traefik, Longhorn
dashboard_admin_user: admin
dashboard_admin_password: CHANGEME

# ============================================================================
# cert-manager / Let's Encrypt Configuration
# ============================================================================
# Email for Let's Encrypt certificate notifications (renewals, expiry warnings)
# This email will receive notifications about certificate lifecycle
certmanager_email: "admin@dzarpelon.com"

# Cloudflare API Token for DNS-01 ACME challenge
# Required permissions: Zone - DNS - Edit for dzarpelon.com
# Create token at: https://dash.cloudflare.com/profile/api-tokens
# Template: "Edit zone DNS"
certmanager_cloudflare_api_token: CHANGEME
